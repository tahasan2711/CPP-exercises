

#include <iostream>
#include <typeinfo>

/*
    Integer:
            - int (32/16 bits), char (8 bits)
            - stdint.h provides portable types: uint32_t, int16_t
    Floating:
            - float , double, long double
    Boolean :
            - type keyword: bool
            - variable value: true/false, any-non-zero/0

    String  : not direct type but rather as class

            - ends with null character

    Pointer :

            - memory address
*/


int main(){
    
    std::cout << std::endl << std::endl;
    return (0);
}
